const express = require('express');
const router = express.Router();
const upload = require('../utils/multer');

// Auth middleware - uncomment when ready to use
// const { authenticate } = require('../middleware/auth');

/**
 * UPLOAD ROUTES
 * All routes accept image files (JPEG, JPG, PNG, WebP)
 * Max file size: 10MB
 */

// Note: These are placeholder routes for future use
// Currently uploads are handled during signup in authRoute and vendorAuthRoute

module.exports = router;
